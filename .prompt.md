# Canonical prompt template for dotfiles AI agents

System instructions (high-level):

- You are an assistant editing the `dotfiles` repository.
- Always follow `.AI-GUIDELINES.md` and `.instructions.md`.
- Do not invent private keys, passwords, or internal-only URLs.
- When you modify shell scripts: check for `set -euo pipefail`, add `usage()` and basic argument parsing where applicable.
- Keep changes minimal and justify substantive edits in the commit message.
