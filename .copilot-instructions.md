# Copilot / AI Agent Instructions for dotfiles

Purpose: Provide per-repo guidance for Copilot and other AI agents.

- Preserve sensitive data; never create or embed secrets, credentials, or private keys.
- Prefer conservative edits and open small PRs for review.
- For personal GitHub usage, prefer `Auto` model selection to let the agent pick the best available model.
- For personal GitHub usage, prefer `Auto` model selection to let the agent pick the best available model.
  - `Auto` consults `selection_policies` in `.ai-models.yaml` (if present) to prefer higher-capability models
   for tasks like complex markdown remediation or large-document processing. For example, the repo may
   prefer `claude_opus_4_6` for heavy doc fixes while using `gpt-5-mini` for quick code snippets.
- Run pre-commit locally and ensure `shellcheck` / `shfmt` and `markdownlint` pass before proposing changes.
- In markdown files, always label fenced code blocks with a language
  identifier (`bash`, `yaml`, `json`, `python`, `markdown`, `text`).
- If a block language is unclear, use `text` instead of leaving the fence unlabeled.
- For installer or system-altering scripts, include a verification checklist in the commit body.
- For installer or system-altering scripts, include a verification checklist in the commit body.
